server:
  port: 8081

spring:
  application:
    name: toy-backend

  datasource:
    url: jdbc:mariadb://141.11.9.2:3306/app_server?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: miv
    password: 1234

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect